#include <string.h>
#include "myio.h"
#pragma  message("use s_gets() from \"myio.h\"")


int main(void){
    char str[STR_LENGTH];
    input_str(str);
    //input_str2(str);
    
    return 0;
}

